This is a NLP chatbot
It uses three of OFF API's  to fetch data from the OpenFoodFacts Database.
To convert the human input to match an API we use RASA which is an OpenSource tool .
For the UI we use React JSX .
TO run , start the react server ,rasa actions server and the rasa server .
